<?xml version="1.0" encoding="utf-8"?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/"><channel><title>GitFR (git)</title><link>http://gitfr.net/blog</link><description></description><atom:link href="http://gitfr.net/categories/git.xml" type="application/rss+xml" rel="self"></atom:link><language>fr</language><lastBuildDate>Sat, 22 Feb 2014 14:49:46 GMT</lastBuildDate><generator>Nikola &lt;http://getnikola.com/&gt;</generator><docs>http://blogs.law.harvard.edu/tech/rss</docs><item><title>Git 1.8.4 publiée</title><link>http://gitfr.net/posts/git-184-publiee.html</link><description>&lt;div&gt;&lt;p&gt;La version 1.8.4 est sortie le 23 août. Beaucoup de choses au menu avec plus de
870 changements (venant de plus de 100 contributeurs). Le changelog est donc
particulièrement bien fourni. Voici quelques points clés :&lt;/p&gt;
&lt;ul class="simple"&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git log&lt;/tt&gt; a une option "-Lbegin,end:filename". Encore expérimentale, elle
permet de spécifier un range (désolé pour l'anglicisme mais j'ai du mal à
trouver une terme français aussi explicite).&lt;/li&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git clean&lt;/tt&gt; possède maintenant un mode interactif qui reprend celui de
&lt;tt class="docutils literal"&gt;git add&lt;/tt&gt;.&lt;/li&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git submodule&lt;/tt&gt; peut être appelé des sous-répertoires, et une nouvelle
variable &lt;tt class="docutils literal"&gt;&lt;span class="pre"&gt;submodule.*.update&lt;/span&gt;&lt;/tt&gt; permet d'invoquer une commande. De plus,
on peut cloner un dépôt en mode &lt;tt class="docutils literal"&gt;shallow&lt;/tt&gt;.&lt;/li&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git rebase &lt;span class="pre"&gt;-i&lt;/span&gt;&lt;/tt&gt; gère maintenant les options &lt;tt class="docutils literal"&gt;strategy&lt;/tt&gt; et &lt;tt class="docutils literal"&gt;&lt;span class="pre"&gt;-X&lt;/span&gt;&lt;/tt&gt;.&lt;/li&gt;
&lt;li&gt;Pendant un &lt;tt class="docutils literal"&gt;git rebase –autostash&lt;/tt&gt;, on peut sauver le stash avant de
lancer l'opération.&lt;/li&gt;
&lt;li&gt;2 nouvelles variables &lt;tt class="docutils literal"&gt;status.branch&lt;/tt&gt; et &lt;tt class="docutils literal"&gt;status.short&lt;/tt&gt; pour éviter de
taper &lt;tt class="docutils literal"&gt;git status &lt;span class="pre"&gt;-branch&lt;/span&gt;&lt;/tt&gt; et &lt;tt class="docutils literal"&gt;git status &lt;span class="pre"&gt;-short&lt;/span&gt;&lt;/tt&gt;.&lt;/li&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git log&lt;/tt&gt; possède une nouvelle option &lt;tt class="docutils literal"&gt;&lt;span class="pre"&gt;–author-date-order&lt;/span&gt;&lt;/tt&gt;.&lt;/li&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git diff&lt;/tt&gt; a un nouveau mode pour ignorer les additions ou suppressions
de lignes vides.&lt;/li&gt;
&lt;li&gt;La variable &lt;tt class="docutils literal"&gt;color.ui&lt;/tt&gt; est maintenant sur &lt;tt class="docutils literal"&gt;auto&lt;/tt&gt;.&lt;/li&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git &lt;span class="pre"&gt;name-rev&lt;/span&gt;&lt;/tt&gt; peut nommer un tag annoté à partir de son nom.&lt;/li&gt;
&lt;li&gt;&lt;tt class="docutils literal"&gt;git describe&lt;/tt&gt; possède maintenant l'option &lt;tt class="docutils literal"&gt;&lt;span class="pre"&gt;–first-parent&lt;/span&gt;&lt;/tt&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;A noter que la version 2.0 devrait sortir l'année prochaine.&lt;/p&gt;
&lt;div class="section" id="changelog"&gt;
&lt;h3&gt;Changelog&lt;/h3&gt;
&lt;blockquote&gt;
&lt;ul&gt;
&lt;li&gt;&lt;p class="first"&gt;Cygwin port has been updated for more recent Cygwin 1.7.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git rebase -i" now honors --strategy and -X options.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Git-gui has been updated to its 0.18.0 version.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;MediaWiki remote helper (in contrib/) has been updated to use the
credential helper interface from Git.pm.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Update build for Cygwin 1.[57].  Torsten Bögershausen reports that
this is fine with Cygwin 1.7 ($gmane/225824) so let's try moving it
ahead.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The credential helper to talk to keychain on OS X (in contrib/) has
been updated to kick in not just when talking http/https but also
imap(s) and smtp.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Remote transport helper has been updated to report errors and
maintain ref hierarchy used to keep track of its own state better.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;With "export" remote-helper protocol, (1) a push that tries to
update a remote ref whose name is different from the pushing side
does not work yet, and (2) the helper may not know how to do
--dry-run; these problematic cases are disabled for now.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;git-remote-hg/bzr (in contrib/) updates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;git-remote-mw (in contrib/) hints users to check the certificate,
when &lt;a class="reference external" href="https://"&gt;https://&lt;/a&gt; connection failed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;git-remote-mw (in contrib/) adds a command to allow previewing the
contents locally before pushing it out, when working with a
MediaWiki remote.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Sample "post-receive-email" hook script got an enhanced replacement
"multimail" (in contrib/).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Also in contrib/ is a new "contacts" script that runs "git blame"
to find out the people who may be interested in a set of changes.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git clean" command learned an interactive mode.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The "--head" option to "git show-ref" was only to add "HEAD" to the
list of candidate refs to be filtered by the usual rules
(e.g. "--heads" that only show refs under refs/heads).  The meaning
of the option has been changed to always show "HEAD" regardless of
what filtering will be applied to any other ref.&lt;/p&gt;
&lt;p&gt;This is a backward incompatible change and might cause breakages to
people's existing scripts.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git show -s" was less discoverable than it should have been.  It
now has a natural synonym "git show --no-patch".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git check-mailmap" is a new command that lets you map usernames
and e-mail addresses through the mailmap mechanism, just like many
built-in commands do.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git name-rev" learned to name an annotated tag object back to its
tagname; "git name-rev $(git rev-parse v1.0.0)" gives "tags/v1.0.0",
for example.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git cat-file --batch-check=&amp;lt;format&amp;gt;" is added, primarily to allow
on-disk footprint of objects in packfiles (often they are a lot
smaller than their true size, when expressed as deltas) to be
reported.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git rebase [-i]" used to leave just "rebase" as its reflog messages
for some operations. They have been reworded to be more informative.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;In addition to the choice from "rebase, merge, or checkout-detach",
"submodule update" can allow a custom command to be used in to
update the working tree of submodules via the "submodule.*.update"
configuration variable.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git submodule update" can optionally clone the submodule
repositories shallowly.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git format-patch" learned "--from[=whom]" option, which sets the
"From: " header to the specified person (or the person who runs the
command, if "=whom" part is missing) and move the original author
information to an in-body From: header as necessary.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The configuration variable "merge.ff" was cleary a tri-state to
choose one from "favor fast-forward when possible", "always create
a merge even when the history could fast-forward" and "do not
create any merge, only update when the history fast-forwards", but
the command line parser did not implement the usual convention of
"last one wins, and command line overrides the configuration"
correctly.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"gitweb" learned to optionally place extra links that point at the
levels higher than the Gitweb pages themselves in the breadcrumbs,
so that it can be used as part of a larger installation.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git log --format=" now honors i18n.logoutputencoding configuration
variable.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The "push.default=simple" mode of "git push" has been updated to
behave like "current" without requiring a remote tracking
information, when you push to a remote that is different from where
you fetch from (i.e. a triangular workflow).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Having multiple "fixup!" on a line in the rebase instruction sheet
did not work very well with "git rebase -i --autosquash".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git log" learned the "--author-date-order" option, with which the
output is topologically sorted and commits in parallel histories
are shown intermixed together based on the author timestamp.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Various subcommands of "git submodule" refused to run from anywhere
other than the top of the working tree of the superproject, but
they have been taught to let you run from a subdirectory.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git diff" learned a mode that ignores hunks whose change consists
only of additions and removals of blank lines, which is the same as
"diff -B" (ignore blank lines) of GNU diff.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git rm" gives a single message followed by list of paths to report
multiple paths that cannot be removed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git rebase" can be told with ":/look for this string" syntax commits
to replay the changes onto and where the work to be replayed begins.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Many tutorials teach users to set "color.ui" to "auto" as the first
thing after you set "user.name/email" to introduce yourselves to
Git.  Now the variable defaults to "auto".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;On Cygwin, "cygstart" is now recognised as a possible way to start
a web browser (used in "help -w" and "instaweb" among others).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git status" learned status.branch and status.short configuration
variables to use --branch and --short options by default (override
with --no-branch and --no-short options from the command line).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git cmd &amp;lt;name&amp;gt;", when &amp;lt;name&amp;gt; happens to be a 40-hex string,
directly uses the 40-hex string as an object name, even if a ref
"refs/&amp;lt;some hierarchy&amp;gt;/&amp;lt;name&amp;gt;" exists.  This disambiguation order
is unlikely to change, but we should warn about the ambiguity just
like we warn when more than one refs/ hierarchies share the same
name.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git rebase" learned "--[no-]autostash" option to save local
changes instead of refusing to run (to which people's normal
response was to stash them and re-run).  This introduced a corner
case breakage to "git am --abort" but it has been fixed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"check-ignore" (new feature since 1.8.2) has been updated to work
more like "check-attr" over bidi-pipes.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git describe" learned "--first-parent" option to limit its closest
tagged commit search to the first-parent chain.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git merge foo" that might have meant "git merge origin/foo" is
diagnosed with a more informative error message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git log -L&amp;lt;line&amp;gt;,&amp;lt;range&amp;gt;:&amp;lt;filename&amp;gt;" has been added.  This may
still have leaks and rough edges, though.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;We used the approxidate() parser for "--expire=&amp;lt;timestamp&amp;gt;" options
of various commands, but it is better to treat --expire=all and
--expire=now a bit more specially than using the current timestamp.
"git gc" and "git reflog" have been updated with a new parsing
function for expiry dates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Updates to completion (both bash and zsh) helpers.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The behaviour of the "--chain-reply-to" option of "git send-email"
have changed at 1.7.0, and we added a warning/advice message to
help users adjust to the new behaviour back then, but we kept it
around for too long.  The message has finally been removed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git fetch origin master" unlike "git fetch origin" or "git fetch"
did not update "refs/remotes/origin/master"; this was an early
design decision to keep the update of remote tracking branches
predictable, but in practice it turns out that people find it more
convenient to opportunistically update them whenever we have a
chance, and we have been updating them when we run "git push" which
already breaks the original "predictability" anyway.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The configuration variable core.checkstat was advertised in the
documentation but the code expected core.statinfo instead.
For now, we accept both core.checkstat and core.statinfo, but the
latter will be removed in the longer term.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;On Cygwin, we used to use our own lstat(2) emulation that is
allegedly faster than the platform one in codepaths where some of
the information it returns did not matter, but it started to bite
us in a few codepaths where the trick it uses to cheat does show
breakages. This emulation has been removed and we use the native
lstat(2) emulation supplied by Cygwin now.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The function attributes extensions are used to catch mistakes in
use of our own variadic functions that use NULL sentinel at the end
(i.e. like execl(3)) and format strings (i.e. like printf(3)).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The code to allow configuration data to be read from in-tree blob
objects is in.  This may help working in a bare repository and
submodule updates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Fetching between repositories with many refs employed O(n^2)
algorithm to match up the common objects, which has been corrected.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The original way to specify remote repository using .git/branches/
used to have a nifty feature.  The code to support the feature was
still in a function but the caller was changed not to call it 5
years ago, breaking that feature and leaving the supporting code
unreachable.  The dead code has been removed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git pack-refs" that races with new ref creation or deletion have
been susceptible to lossage of refs under right conditions, which
has been tightened up.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;We read loose and packed references in two steps, but after
deciding to read a loose ref but before actually opening it to read
it, another process racing with us can unlink it, which would cause
us to barf.  The codepath has been updated to retry when such a
race is detected, instead of outright failing.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Uses of the platform fnmatch(3) function (many places in the code,
matching pathspec, .gitignore and .gitattributes to name a few)
have been replaced with wildmatch, allowing "foo/**/bar" that would
match foo/bar, foo/a/bar, foo/a/b/bar, etc.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Memory ownership and lifetime rules for what for-each-ref feeds to
its callbacks have been clarified (in short, "you do not own it, so
make a copy if you want to keep it").&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The revision traversal logic to improve culling of irrelevant
parents while traversing a mergy history has been updated.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Some leaks in unpack-trees (used in merge, cherry-pick and other
codepaths) have been plugged.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The codepath to read from marks files in fast-import/export did not
have to accept anything but 40-hex representation of the object
name.  Further, fast-export did not need full in-core object
representation to have parsed wen reading from them.  These
codepaths have been optimized by taking advantage of these access
patterns.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Object lookup logic, when the object hashtable starts to become
crowded, has been optimized.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;When TEST_OUTPUT_DIRECTORY setting is used, it was handled somewhat
inconsistently between the test framework and t/Makefile, and logic
to summarize the results looked at a wrong place.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git clone" uses a lighter-weight implementation when making sure
that the history behind refs are complete.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Many warnings from sparse source checker in compat/ area has been
squelched.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The code to reading and updating packed-refs file has been updated,
correcting corner case bugs.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Newer Net::SMTP::SSL module does not want the user programs to use
the default behaviour to let server certificate go without
verification, so by default enable the verification with a
mechanism to turn it off if needed.
(merge 35035bb rr/send-email-ssl-verify later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;When "git" is spawned in such a way that any of the low 3 file
descriptors is closed, our first open() may yield file descriptor 2,
and writing error message to it would screw things up in a big way.
(merge a11c396 tr/protect-low-3-fds later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The mailmap mechanism unnecessarily downcased the e-mail addresses
in the output, and also ignored the human name when it is a single
character name.
(merge bd23794 jc/mailmap-case-insensitivity later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;In two places we did not check return value (expected to be a file
descriptor) correctly.
(merge a77f106 tr/fd-gotcha-fixes later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Logic to auto-detect character encodings in the commit log message
did not reject overlong and invalid UTF-8 characters.
(merge 81050ac bc/commit-invalid-utf8 later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Pass port number as a separate argument when "send-email" initializes
Net::SMTP, instead of as a part of the hostname, i.e. host:port.
This allows GSSAPI codepath to match with the hostname given.
(merge 1a741bf bc/send-email-use-port-as-separate-param later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git diff" refused to even show difference when core.safecrlf is
set to true (i.e. error out) and there are offending lines in the
working tree files.
(merge 5430bb2 jc/maint-diff-core-safecrlf later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;A test that should have failed but didn't revealed a bug that needs
to be corrected.
(merge 94d75d1 jc/t1512-fix later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;An overlong path to a .git directory may have overflown the
temporary path buffer used to create a name for lockfiles.
(merge 2fbd4f9 mh/maint-lockfile-overflow later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Invocations of "git checkout" used internally by "git rebase" were
counted as "checkout", and affected later "git checkout -" to the
the user to an unexpected place.
(merge 3bed291 rr/rebase-checkout-reflog later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The configuration variable column.ui was poorly documented.
(merge 5e62cc1 rr/column-doc later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git name-rev --refs=tags/v*" were forbidden, which was a bit
inconvenient (you had to give a pattern to match refs fully, like
--refs=refs/tags/v*).
(merge 98c5c4a nk/name-rev-abbreviated-refs later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git apply" parsed patches that add new files, generated by
programs other than Git, incorrectly.  This is an old breakage in
v1.7.11 and will need to be merged down to the maintenance tracks.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Older cURL wanted piece of memory we call it with to be stable, but
we updated the auth material after handing it to a call.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git pull" into nothing trashed "local changes" that were in the
index, and this avoids it.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Many "git submodule" operations do not work on a submodule at a
path whose name is not in ASCII.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"cherry-pick" had a small leak in an error codepath.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Logic used by git-send-email to suppress cc mishandled names like
"A U. Thor" &amp;lt;&lt;a class="reference external" href="mailto:author@example.xz"&gt;author@example.xz&lt;/a&gt;&amp;gt;, where the human readable part
needs to be quoted (the user input may not have the double quotes
around the name, and comparison was done between quoted and
unquoted strings).  It also mishandled names that need RFC2047
quoting.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Call to discard_cache/discard_index (used when we use different
contents of the index in-core, in many operations like commit,
apply, and merge) used to leak memory that held the array of index
entries, which has been plugged.
(merge a0fc4db rs/discard-index-discard-array later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"gitweb" forgot to clear a global variable $search_regexp upon each
request, mistakenly carrying over the previous search to a new one
when used as a persistent CGI.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The wildmatch engine did not honor WM_CASEFOLD option correctly.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git log -c --follow $path" segfaulted upon hitting the commit that
renamed the $path being followed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;When a reflog notation is used for implicit "current branch", we
did not say which branch and worse said "branch ''".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"difftool --dir-diff" did not copy back changes made by the
end-user in the diff tool backend to the working tree in some
cases.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git push $there HEAD:branch" did not resolve HEAD early enough, so
it was easy to flip it around while push is still going on and push
out a branch that the user did not originally intended when the
command was started.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;The bash prompt code (in contrib/) displayed the name of the branch
being rebased when "rebase -i/-m/-p" modes are in use, but not the
plain vanilla "rebase".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Handling of negative exclude pattern for directories "!dir" was
broken in the update to v1.8.3.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;zsh prompt script that borrowed from bash prompt script did not
work due to slight differences in array variable notation between
these two shells.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;An entry for "&lt;a class="reference external" href="file://"&gt;file://&lt;/a&gt;" scheme in the enumeration of URL types Git
can take in the HTML documentation was made into a clickable link
by mistake.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git push --[no-]verify" was not documented.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Stop installing the git-remote-testpy script that is only used for
testing.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git commit --allow-empty-message -m ''" should not start an
editor.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git merge @{-1}~22" was rewritten to "git merge &lt;a class="reference external" href="mailto:frotz@%7B1%7D~22"&gt;frotz@{1}~22&lt;/a&gt;"
incorrectly when your previous branch was "frotz" (it should be
rewritten to "git merge frotz~22" instead).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git diff -c -p" was not showing a deleted line from a hunk when
another hunk immediately begins where the earlier one ends.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git log --ancestry-path A...B" did not work as expected, as it did
not pay attention to the fact that the merge base between A and B
was the bottom of the range being specified.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Mac OS X does not like to write(2) more than INT_MAX number of
bytes; work it around by chopping write(2) into smaller pieces.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;Newer MacOS X encourages the programs to compile and link with
their CommonCrypto, not with OpenSSL.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git clone foo/bar:baz" cannot be a request to clone from a remote
over git-over-ssh specified in the scp style.  This case is now
detected and clones from a local repository at "foo/bar:baz".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;When $HOME is misconfigured to point at an unreadable directory, we
used to complain and die. Loosen the check.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git subtree" (in contrib/) had one codepath with loose error
checks to lose data at the remote side.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git fetch" into a shallow repository from a repository that does
not know about the shallow boundary commits (e.g. a different fork
from the repository the current shallow repository was cloned from)
did not work correctly.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;&lt;p class="first"&gt;"git checkout foo" DWIMs the intended "upstream" and turns it into
"git checkout -t -b foo remotes/origin/foo". This codepath has been
updated to correctly take existing remote definitions into account.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
&lt;/div&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-184-publiee.html</guid><pubDate>Mon, 02 Sep 2013 08:34:06 GMT</pubDate></item><item><title>Git 1.8.0 publiée</title><link>http://gitfr.net/posts/git-1.8.0-publiee.html</link><description>&lt;div&gt;&lt;p&gt;&lt;strong&gt;Note&lt;/strong&gt; : Git 1.8.0 est sortie le 22 octobre, désolé pour le retard.&lt;/p&gt;
&lt;p&gt;Changements intéressants:
 * la configuration upstream d'une branche évolue: --set-upstream est dépréciée,
   et est remplacée par --set-upstream-to=reference.
   Au delà du changement de nom, c'est la syntaxe qui évolue pour être plus simple
   et plus logique.
   En effet, avec --set-upstream, vous deviez taper:&lt;/p&gt;
&lt;p&gt;&lt;code&gt;git branch --set-upstream my-branch origin/dev&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;--set-upstream est un flag, qui précise que le refspec de base de la branche
   créée est aussi l'upstream. Ce n'est pas évident, et l'on aurait tendance à
   interpréter la commande dans l'autre sens, dans la tradition des commandes Unix.
   Avec la nouvelle syntaxe, on tape:&lt;/p&gt;
&lt;p&gt;&lt;code&gt;git branch --set-upstream-to=origin/dev my-branch&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Cette syntaxe est bien plus compréhensible. L'ancienne syntaxe est dépréciée.
   Si vous ne connaissez toujours pas les magies et bienfaits des branches upstream,
   je vous invite à (re)lire
   &lt;a href="http://gitfr.net/blog/2011/12/11/comportement-par-defaut-du-git-push/"&gt;ce billet&lt;/a&gt;.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;git cherry-pick respecte désormais l'ordre des commits tels que passés en arguments.
   Précédemment, il rejouait les commits suivant la chronologie de commit.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;git blame est désormais plus précis lors des merge : il reconnait les conflits
   résolus comme venant d'un des commits parents plutôt que simplement étant des
   modifications non committées (sha1=0000000)&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;N'hésitez pas comme d'habitude à commenter ce billet si vous voyez des
changements intéressants.&lt;/p&gt;
&lt;h4&gt;Changelog&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;A credential helper for Win32 to allow access to the keychain of
   the logged-in user has been added.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;An initial port to HP NonStop.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A credential helper to allow access to the Gnome keyring has been
   added.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When "git am" sanitizes the "Subject:" line, we strip the prefix from
   "Re: subject" and also from a less common "re: subject", but left
   the even less common "RE: subject" intact.  Now we strip that too.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;It was tempting to say "git branch --set-upstream origin/master",
   but that tells Git to arrange the local branch "origin/master" to
   integrate with the currently checked out branch, which is highly
   unlikely what the user meant.  The option is deprecated; use the
   new "--set-upstream-to" (with a short-and-sweet "-u") option
   instead.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git cherry-pick" learned the "--allow-empty-message" option to
   allow it to replay a commit without any log message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;After "git cherry-pick -s" gave control back to the user asking
   help to resolve conflicts, concluding "git commit" used to need to
   be run with "-s" if the user wants to sign it off; now the command
   leaves the sign-off line in the log template.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git daemon" learned the "--access-hook" option to allow an
   external command to decline service based on the client address,
   repository path, etc.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git difftool --dir-diff" learned to use symbolic links to prepare
   a temporary copy of the working tree when available.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git grep" learned to use a non-standard pattern type by default if
   a configuration variable tells it to.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Accumulated updates to "git gui" has been merged.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log -g" learned the "--grep-reflog=&lt;pattern&gt;" option to limit
   its output to commits with a reflog message that matches the given
   pattern.&lt;/pattern&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git merge-base" learned the "--is-ancestor A B" option to tell if A is
   an ancestor of B.  The result is indicated by its exit status code.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git mergetool" now allows users to override the actual command used
   with the mergetool.$name.cmd configuration variable even for built-in
   mergetool backends.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rebase -i" learned the "--edit-todo" option to open an editor
   to edit the instruction sheet.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git svn" has been updated to work with SVN 1.7.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git p4" learned the "--conflicts" option to specify what to do when
   encountering a conflict during "p4 submit".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Git ships with a fall-back regexp implementation for platforms with
   buggy regexp library, but it was easy for people to keep using their
   platform regexp by mistake.  A new test has been added to check this.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The "check-docs" build target has been updated and greatly
   simplified.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The test suite is run under MALLOC_CHECK_ when running with a glibc
   that supports the feature.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The documentation in the TeXinfo format was using indented output
   for materials meant to be examples that are better typeset in
   monospace.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Compatibility wrapper around some mkdir(2) implementations that
   reject parameters with trailing slash has been introduced.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Compatibility wrapper for systems that lack usable setitimer() has
   been added.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The option parsing of "git checkout" had error checking, dwim and
   defaulting missing options, all mixed in the code, and issuing an
   appropriate error message with useful context was getting harder.
   The code has been reorganized to allow giving a proper diagnosis
   when the user says "git checkout -b -t foo bar" (e.g. "-t" is not a
   good name for a branch).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Many internal uses of a "git merge-base" equivalent were only to see
   if one commit fast-forwards to the other, which did not need the
   full set of merge bases to be computed. They have been updated to
   use less expensive checks.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The heuristics to detect and silently convert latin1 to utf8 when
   we were told to use utf-8 in the log message has been transplanted
   from "mailinfo" to "commit" and "commit-tree".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Messages given by "git &lt;subcommand&gt; -h" from many subcommands have
   been marked for translation.&lt;/subcommand&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The attribute system may be asked for a path that itself or its
   leading directories no longer exists in the working tree, and it is
   fine if we cannot open .gitattribute file in such a case.  Failure
   to open per-directory .gitattributes with error status other than
   ENOENT and ENOTDIR should be diagnosed, but it wasn't.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When looking for $HOME/.gitconfig etc., it is OK if we cannot read
   them because they do not exist, but we did not diagnose existing
   files that we cannot read.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When "git am" is fed an input that has multiple "Content-type: ..."
   header, it did not grok charset= attribute correctly.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git am" mishandled a patch attached as application/octet-stream
   (e.g. not text/*); Content-Transfer-Encoding (e.g. base64) was not
   honored correctly.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git blame MAKEFILE" run in a history that has "Makefile" but not
   "MAKEFILE" should say "No such file MAKEFILE in HEAD", but got
   confused on a case insensitive filesystem and failed to do so.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Even during a conflicted merge, "git blame $path" always meant to
   blame uncommitted changes to the "working tree" version; make it
   more useful by showing cleanly merged parts as coming from the other
   branch that is being merged.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;It was unclear in the documentation for "git blame" that it is
   unnecessary for users to use the "--follow" option.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Output from "git branch -v" contains "(no branch)" that could be
   localized, but the code to align it along with the names of
   branches was counting in bytes, not in display columns.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git cherry-pick A C B" used to replay changes in A and then B and
   then C if these three commits had committer timestamps in that
   order, which is not what the user who said "A C B" naturally
   expects.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A repository created with "git clone --single" had its fetch
   refspecs set up just like a clone without "--single", leading the
   subsequent "git fetch" to slurp all the other branches, defeating
   the whole point of specifying "only this branch".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Documentation talked about "first line of commit log" when it meant
   the title of the commit.  The description was clarified by defining
   how the title is decided and rewording the casual mention of "first
   line" to "title".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git cvsimport" did not thoroughly cleanse tag names that it
   inferred from the names of the tags it obtained from CVS, which
   caused "git tag" to barf and stop the import in the middle.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Earlier we made the diffstat summary line that shows the number of
   lines added/deleted localizable, but it was found irritating having
   to see them in various languages on a list whose discussion language
   is English, and this change has been reverted.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch --all", when passed "--no-tags", did not honor the
   "--no-tags" option while fetching from individual remotes (the same
   issue existed with "--tags", but the combination "--all --tags" makes
   much less sense than "--all --no-tags").&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch" over http had an old workaround for an unlikely server
   misconfiguration; it turns out that this hurts debuggability of the
   configuration in general, and has been reverted.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch" over http advertised that it supports "deflate", which
   is much less common, and did not advertise the more common "gzip" on
   its Accept-Encoding header.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch" over the dumb-http revision walker could segfault when
   curl's multi interface was used.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git gc --auto" notified the user that auto-packing has triggered
    even under the "--quiet" option.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;After "gitk" showed the contents of a tag, neither "Reread
   references" nor "Reload" updated what is shown as the
   contents of it when the user overwrote the tag with "git tag -f".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log --all-match --grep=A --grep=B" ought to show commits that
   mention both A and B, but when these three options are used with
   --author or --committer, it showed commits that mention either A or
   B (or both) instead.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The "-Xours" backend option to "git merge -s recursive" was ignored
   for binary files.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git p4", when "--use-client-spec" and "--detect-branches" are used
   together, misdetected branches.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git receive-pack" (the counterpart to "git push") did not give
   progress output while processing objects it received to the puser
   when run over the smart-http protocol.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When you misspell the command name you give to the "exec" action in
   the "git rebase -i" instruction sheet you were told that 'rebase' is not a
   git subcommand from "git rebase --continue".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The subcommand in "git remote" to remove a defined remote was
   "rm" and the command did not take a fully-spelled "remove".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The interactive prompt that "git send-email" gives was error prone. It
   asked "What e-mail address do you want to use?" with the address it
   guessed (correctly) the user would want to use in its prompt,
   tempting the user to say "y". But the response was taken as "No,
   please use 'y' as the e-mail address instead", which is most
   certainly not what the user meant.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git show --format='%ci'" did not give the timestamp correctly for
   commits created without human readable name on the "committer" line.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git show --quiet" ought to be a synonym for "git show -s", but
   wasn't.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git submodule frotz" was not diagnosed as "frotz" being an unknown
   subcommand to "git submodule"; the user instead got a complaint
   that "git submodule status" was run with an unknown path "frotz".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git status" honored the ignore=dirty settings in .gitmodules but
   "git commit" didn't.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" did not give the correct committer timezone in its feed
   output due to a typo.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-1.8.0-publiee.html</guid><pubDate>Sun, 06 Jan 2013 21:43:00 GMT</pubDate></item><item><title>Git 1.7.12 publiée</title><link>http://gitfr.net/posts/git-1.7.12-publiee.html</link><description>&lt;div&gt;&lt;p&gt;Quatre changements intéressants dans cette 1.7.12 qui date du 20 août :&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Une option &lt;code&gt;--root&lt;/code&gt; à rebase pour modifier aussi le commit root.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Une option &lt;code&gt;-x&lt;/code&gt; à rebase pour éxécuter une commande à chaque
  application d'un patch.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;On peut visualiser la documentation en HTML avec &lt;code&gt;git help -w $cmd&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;git status&lt;/code&gt; est plus précis quand le dépôt est dans un état
  intermédiaire (lors d'un merge, un rebase, etc).&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;N'hésitez pas comme d'habitude à commenter ce billet si vous voyez des 
changements intéressants.&lt;/p&gt;
&lt;h4&gt;Le changelog&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;Git can be told to normalize pathnames it read from readdir(3) and
   all arguments it got from the command line into precomposed UTF-8
   (assuming that they come as decomposed UTF-8), in order to work
   around issues on Mac OS.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;I think there still are other places that need conversion
   (e.g. paths that are read from stdin for some commands), but this
   should be a good first step in the right direction.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Per-user $HOME/.gitconfig file can optionally be stored in
   $HOME/.config/git/config instead, which is in line with XDG.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The value of core.attributesfile and core.excludesfile default to
   $HOME/.config/git/attributes and $HOME/.config/git/ignore respectively
   when these files exist.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Logic to disambiguate abbreviated object names have been taught to
   take advantage of object types that are expected in the context,
   e.g. XXXXXX in the "git describe" output v1.2.3-gXXXXXX must be a
   commit object, not a blob nor a tree.  This will help us prolong
   the lifetime of abbreviated object names.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git apply" learned to wiggle the base version and perform three-way
   merge when a patch does not exactly apply to the version you have.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Scripted Porcelain writers now have access to the credential API via
   the "git credential" plumbing command.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git help" used to always default to "man" format even on platforms
   where "man" viewer is not widely available.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git clone --local $path" started its life as an experiment to
   optionally use link/copy when cloning a repository on the disk, but
   we didn't deprecate it after we made the option a no-op to always
   use the optimization.  The command learned "--no-local" option to
   turn this off, as a more explicit alternative over use of file://
   URL.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch" and friends used to say "remote side hung up
   unexpectedly" when they failed to get response they expect from the
   other side, but one common reason why they don't get expected
   response is that the remote repository does not exist or cannot be
   read. The error message in this case was updated to give better
   hints to the user.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git help -w $cmd" can show HTML version of documentation for
   "git-$cmd" by setting help.htmlpath to somewhere other than the
   default location where the build procedure installs them locally;
   the variable can even point at a http:// URL.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rebase [-i] --root $tip" can now be used to rewrite all the
   history leading to "$tip" down to the root commit.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rebase -i" learned "-x &lt;cmd&gt;" to insert "exec &lt;cmd&gt;" after
   each commit in the resulting history.&lt;/cmd&gt;&lt;/cmd&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git status" gives finer classification to various states of paths
   in conflicted state and offer advice messages in its output.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git submodule" learned to deal with nested submodule structure
   where a module is contained within a module whose origin is
   specified as a relative URL to its superproject's origin.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A rather heavy-ish "git completion" script has been split to create
   a separate "git prompting" script, to help lazy-autoloading of the
   completion part while making prompting part always available.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" pays attention to various forms of credits that are
   similar to "Signed-off-by:" lines in the commit objects and
   highlights them accordingly.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"mediawiki" remote helper (in contrib/) learned to handle file
   attachments.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git p4" now uses "Jobs:" and "p4 move" when appropriate.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;vcs-svn has been updated to clean-up compilation, lift 32-bit
   limitations, etc.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Some tests showed false failures caused by a bug in ecryptofs.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;We no longer use AsciiDoc7 syntax in our documentation and favor a
   more modern style.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git am --rebasing" codepath was taught to grab authorship, log
   message and the patch text directly out of existing commits.  This
   will help rebasing commits that have confusing "diff" output in
   their log messages.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git index-pack" and "git pack-objects" use streaming API to read
   from the object store to avoid having to hold a large blob object
   in-core while they are doing their thing.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Code to match paths with exclude patterns learned to avoid calling
   fnmatch() by comparing fixed leading substring literally when
   possible.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log -n 1 -- rarely-touched-path" was spending unnecessary
   cycles after showing the first change to find the next one, only to
   discard it.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git svn" got a large-looking code reorganization at the last
   minute before the code freeze.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git submodule add" was confused when the superproject did not have
   its repository in its usual place in the working tree and GIT_DIR
   and GIT_WORK_TREE was used to access it.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit --amend" let the user edit the log message and then died
   when the human-readable committer name was given insufficiently by
   getpwent(3).&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-1.7.12-publiee.html</guid><pubDate>Fri, 24 Aug 2012 02:54:00 GMT</pubDate></item><item><title>Git 1.7.11 publiée</title><link>http://gitfr.net/posts/git-1.7.11-publiee.html</link><description>&lt;div&gt;&lt;p&gt;Deux changements intéressants dans cette 1.7.11 qui date du 18 juin :&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Fini la configuration par défaut qui pousse toutes les branches connues
  sur le serveur. C'est maintenant le mode &lt;em&gt;simple&lt;/em&gt; qui prend le relais,
  mode qui ne pousse (si et seulement si elle possède un nom identique sur le
  serveur) que le branche courante. C'est une configuration bien plus
  pertinente pour les débutants. Néanmoins je vous préconise &lt;strong&gt;le mode
  upstream&lt;/strong&gt; comme dit dans
  &lt;a href="http://gitfr.net/blog/2011/12/11/comportement-par-defaut-du-git-push/"&gt;ce billet&lt;/a&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Un outil en shell du nom de &lt;em&gt;git-subtree.sh&lt;/em&gt; est ajoutée dans la section
  contrib, outil qui simplifie le découpage ou la fusion de dépôt. Cela
  permet par exemple de se dispenser de submodules en intégrant un dépôt
  dans un autre dépôt.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;N'hésitez pas comme d'habitude à commenter ce billet si vous voyez des 
changements intéressants.&lt;/p&gt;
&lt;h4&gt;Le changelog&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;A new mode for push, "simple", which is a cross between "current"
  and "upstream", has been introduced. "git push" without any refspec
  will push the current branch out to the same name at the remote
  repository only when it is set to track the branch with the same
  name over there.  The plan is to make this mode the new default
  value when push.default is not configured.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A couple of commands learned the "--column" option to produce
  columnar output.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A third-party tool "git subtree" is distributed in contrib/&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A remote helper that acts as a proxy and caches ssl session for the
  https:// transport is added to the contrib/ area.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Error messages given when @{u} is used for a branch without its
  upstream configured have been clarified.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Even with the "-q"uiet option, "checkout" used to report setting up
  tracking.  Also "branch" learned the "-q"uiet option to squelch
  informational message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Your build platform may support hardlinks but you may prefer not to
  use them, e.g. when installing to DESTDIR to make a tarball and
  untarring on a filesystem that has poor support for hardlinks.
  There is a Makefile option NO_INSTALL_HARDLINKS for you.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The smart-http backend used to always override GIT_COMMITTER_*
  variables with REMOTE_USER and REMOTE_ADDR, but these variables are
  now preserved when set.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git am" learned the "--include" option, which is an opposite of
  existing the "--exclude" option.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When "git am -3" needs to fall back to an application of the patch
  to a synthesized preimage followed by a 3-way merge, the paths that
  needed such treatment are now reported to the end user, so that the
  result in them can be eyeballed with extra care.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The output from "diff/log --stat" used to always allocate 4 columns
  to show the number of modified lines, but not anymore.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git difftool" learned the "--dir-diff" option to spawn external
  diff tools that can compare two directory hierarchies at a time
  after populating two temporary directories, instead of running an
  instance of the external tool once per a file pair.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The "fmt-merge-msg" command learned to list the primary contributors
  involved in the side topic you are merging in a comment in the merge
  commit template.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rebase" learned to optionally keep commits that do not
  introduce any change in the original history.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git push --recurse-submodules" learned to optionally look into the
  histories of submodules bound to the superproject and push them
  out.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A 'snapshot' request to "gitweb" honors If-Modified-Since: header,
  based on the commit date.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" learned to highlight the patch it outputs even more.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git svn" used to die with unwanted SIGPIPE when talking with an HTTP
  server that uses keep-alive.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git svn" learned to use platform specific authentication
  providers, e.g. gnome-keyring, kwallet, etc.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git p4" has been moved out of the contrib/ area and has seen more
  work on importing labels as tags from (and exporting tags as labels
  to) p4.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Bash completion script (in contrib/) have been cleaned up to make
  future work on it simpler.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;An experimental "version 4" format of the index file has been
  introduced to reduce on-disk footprint and I/O overhead.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git archive" learned to produce its output without reading the
  blob object it writes out in memory in its entirety.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git index-pack" that runs when fetching or pushing objects to
  complete the packfile on the receiving end learned to use multiple
  threads to do its job when available.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The code to compute hash values for lines used by the internal diff
  engine was optimized on little-endian machines, using the same
  trick the kernel folks came up with.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git apply" had some memory leaks plugged.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Setting up a revision traversal with many starting points was
  inefficient as these were placed in a date-order priority queue
  one-by-one.  Now they are collected in the queue unordered first,
  and sorted immediately before getting used.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;More lower-level commands learned to use the streaming API to read
  from the object store without keeping everything in core.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The weighting parameters to suggestion command name typo have been
  tweaked, so that "git tags" will suggest "tag?" and not "stage?".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Because "sh" on the user's PATH may be utterly broken on some
  systems, run-command API now uses SHELL_PATH, not /bin/sh, when
  spawning an external command (not applicable to Windows port).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The API to iterate over the refs/ hierarchy has been tweaked to
  allow walking only a subset of it more efficiently.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git submodule init" used to report "registered for path ..."
  even for submodules that were registered earlier.
  (cherry-pick c1c259e jl/submodule-report-new-path-once later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git diff --stat" used to fully count a binary file with modified
  execution bits whose contents is unmodified, which was not quite
  right.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-1.7.11-publiee.html</guid><pubDate>Sun, 01 Jul 2012 02:52:00 GMT</pubDate></item><item><title>Git 1.7.10 publiée</title><link>http://gitfr.net/posts/git-1.7.10-publiee.html</link><description>&lt;div&gt;&lt;p&gt;Pas mal de petites changements dans cette version 1.7.10 sortie le 10 avril. Je
voulais initialement tester cette version avant de publier l'annonce mais je
n'ai pas eu le cou^H^H^Htemps :(. Mais rapidement de ce qu'ai pu voir :&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;gitk a pas mal de petites évolutions.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Un éditeur s'ouvre lors d'un &lt;code&gt;git-commit&lt;/code&gt; pour pousser les gens à expliquer
  les changements.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;On peut cloner qu'un répertoire.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;N'hésitez pas comme d'habitude à commenter ce billet si vous voyez des 
changements intéressants dans votre façon de travailler avec Git.&lt;/p&gt;
&lt;h4&gt;Le changelog&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;various "gitk" updates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Teams for localizing the messages from the Porcelain layer of
  commands are starting to form, thanks to Jiang Xin who volunteered
  to be the localization coordinator.  Translated messages for
  simplified Chinese, Swedish and Portuguese are available.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The configuration mechanism learned an "include" facility; an
  assignment to the include.path pseudo-variable causes the named
  file to be included in-place when Git looks up configuration
  variables.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A content filter (clean/smudge) used to be just a way to make the
  recorded contents "more useful", and allowed to fail; a filter can
  now optionally be marked as "required".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Options whose names begin with "--no-" (e.g. the "--no-verify"
  option of the "git commit" command) can be negated by omitting
  "no-" from its name, e.g. "git commit --verify".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git am" learned to pass "-b" option to underlying "git mailinfo", so
  that a bracketed string other than "PATCH" at the beginning can be kept.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git clone" learned "--single-branch" option to limit cloning to a
  single branch (surprise!); tags that do not point into the history
  of the branch are not fetched.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git clone" learned to detach the HEAD in the resulting repository
  when the user specifies a tag with "--branch" (e.g., "--branch=v1.0").
  Clone also learned to print the usual "detached HEAD" advice in such
  a case, similar to "git checkout v1.0".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When showing a patch while ignoring whitespace changes, the context
  lines are taken from the postimage, in order to make it easier to
  view the output.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git diff --stat" learned to adjust the width of the output on
  wider terminals, and give more columns to pathnames as needed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"diff-highlight" filter (in contrib/) was updated to produce more
  aesthetically pleasing output.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"fsck" learned "--no-dangling" option to omit dangling object
  information.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log -G" and "git log -S" learned to pay attention to the "-i"
  option.  With "-i", "log -G" ignores the case when finding patch
  hunks that introduce or remove a string that matches the given
  pattern.  Similarly with "-i", "log -S" ignores the case when
  finding the commit the given block of text appears or disappears
  from the file.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git merge" in an interactive session learned to spawn the editor
  by default to let the user edit the auto-generated merge message,
  to encourage people to explain their merges better. Legacy scripts
  can export GIT_MERGE_AUTOEDIT=no to retain the historical behavior.
  Both "git merge" and "git pull" can be given --no-edit from the
  command line to accept the auto-generated merge message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The advice message given when the user didn't give enough clue on
  what to merge to "git pull" and "git merge" has been updated to
  be more concise and easier to understand.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git push" learned the "--prune" option, similar to "git fetch".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The whole directory that houses a top-level superproject managed by
  "git submodule" can be moved to another place.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git symbolic-ref" learned the "--short" option to abbreviate the
  refname it shows unambiguously.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git tag --list" can be given "--points-at &lt;object&gt;" to limit its
  output to those that point at the given object.&lt;/object&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" allows intermediate entries in the directory hierarchy
  that leads to a project to be clicked, which in turn shows the
  list of projects inside that directory.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" learned to read various pieces of information for the
  repositories lazily, instead of reading everything that could be
  needed (including the ones that are not necessary for a specific
  task).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Project search in "gitweb" shows the substring that matched in the
  project name and description highlighted.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;HTTP transport learned to authenticate with a proxy if needed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;A new script "diffall" is added to contrib/; it drives an
  external tool to perform a directory diff of two Git revisions
  in one go, unlike "difftool" that compares one file at a time.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Improved handling of views, labels and branches in "git-p4" (in contrib).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git-p4" (in contrib) suffered from unnecessary merge conflicts when
  p4 expanded the embedded $RCS$-like keywords; it can be now told to
  unexpand them.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Some "git-svn" updates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"vcs-svn"/"svn-fe" learned to read dumps with svn-deltas and
  support incremental imports.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git difftool/mergetool" learned to drive DeltaWalker.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Unnecessary calls to parse_object() "git upload-pack" makes in
  response to "git fetch", have been eliminated, to help performance
  in repositories with excessive number of refs.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Recursive call chains in "git index-pack" to deal with long delta
  chains have been flattened, to reduce the stack footprint.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Use of add_extra_ref() API is now gone, to make it possible to
  cleanly restructure the overall refs API.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The command line parser of "git pack-objects" now uses parse-options
  API.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The test suite supports the new "test_pause" helper function.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Parallel to the test suite, there is a beginning of performance
  benchmarking framework.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;t/Makefile is adjusted to prevent newer versions of GNU make from
  running tests in seemingly random order.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The code to check if a path points at a file beyond a symbolic link
  has been restructured to be thread-safe.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When pruning directories that has become empty during "git prune"
  and "git prune-packed", call closedir() that iterates over a
  directory before rmdir() it.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Build with NO_PERL_MAKEMAKER was broken and Git::I18N did not work
  with versions of Perl older than 5.8.3.
  (merge 5eb660e ab/perl-i18n later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git tag -s" honored "gpg.program" configuration variable since
  1.7.9, but "git tag -v" and "git verify-tag" didn't.
  (merge a2c2506 az/verify-tag-use-gpg-config later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"configure" script learned to take "--with-sane-tool-path" from the
  command line to record SANE_TOOL_PATH (used to avoid broken platform
  tools in /usr/bin) in config.mak.autogen.  This may be useful for
  people on Solaris who have saner tools outside /usr/xpg[46]/bin.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;zsh port of bash completion script needed another workaround.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;git-gui updated to 0.16.0.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;git-p4 (in contrib/) updates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Git uses gettext to translate its most common interface messages
  into the user's language if translations are available and the
  locale is appropriately set. Distributors can drop new PO files
  in po/ to add new translations.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The code to handle username/password for HTTP transactions used in
  "git push" &amp;amp; "git fetch" learned to talk "credential API" to
  external programs to cache or store them, to allow integration with
  platform native keychain mechanisms.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The input prompts in the terminal use our own getpass() replacement
  when possible. HTTP transactions used to ask for the username without
  echoing back what was typed, but with this change you will see it as
  you type.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The internals of "revert/cherry-pick" have been tweaked to prepare
  building more generic "sequencer" on top of the implementation that
  drives them.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rev-parse FETCH_HEAD" after "git fetch" without specifying
  what to fetch from the command line will now show the commit that
  would be merged if the command were "git pull".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git add" learned to stream large files directly into a packfile
  instead of writing them into individual loose object files.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git checkout -B &lt;current branch&gt; &lt;elsewhere&gt;" is a more intuitive
  way to spell "git reset --keep &lt;elsewhere&gt;".&lt;/elsewhere&gt;&lt;/elsewhere&gt;&lt;/current&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git checkout" and "git merge" learned "--no-overwrite-ignore" option
  to tell Git that untracked and ignored files are not expendable.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit --amend" learned "--no-edit" option to say that the
  user is amending the tree being recorded, without updating the
  commit log message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit" and "git reset" re-learned the optimization to prime
  the cache-tree information in the index, which makes it faster to
  write a tree object out after the index entries are updated.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit" detects and rejects an attempt to stuff NUL byte in
  the commit log message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit" learned "-S" to GPG-sign the commit; this can be shown
  with the "--show-signature" option to "git log".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;fsck and prune are relatively lengthy operations that still go
  silent while making the end-user wait. They learned to give progress
  output like other slow operations.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The set of built-in function-header patterns for various languages
  knows MATLAB.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log --format='&lt;format&gt;'" learned new %g[nNeE] specifiers to
  show information from the reflog entries when walking the reflog
  (i.e. with "-g").&lt;/format&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git pull" can be used to fetch and merge an annotated/signed tag,
  instead of the tip of a topic branch. The GPG signature from the
  signed tag is recorded in the resulting merge commit for later
  auditing.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log" learned "--show-signature" option to show the signed tag
  that was merged that is embedded in the merge commit. It also can
  show the signature made on the commit with "git commit -S".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git branch --edit-description" can be used to add descriptive text
  to explain what a topic branch is about.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fmt-merge-msg" learned to take the branch description into
  account when preparing a merge summary that "git merge" records
  when merging a local branch.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git request-pull" has been updated to convey more information
  useful for integrators to decide if a topic is worth merging and
  what is pulled is indeed what the requestor asked to pull,
  including:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;the tip of the branch being requested to be merged;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;the branch description describing what the topic is about;&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;the contents of the annotated tag, when requesting to pull a tag.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git pull" learned to notice 'pull.rebase' configuration variable,
  which serves as a global fallback for setting 'branch.&lt;name&gt;.rebase'
  configuration variable per branch.&lt;/name&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git tag" learned "--cleanup" option to control how the whitespaces
  and empty lines in tag message are cleaned up.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" learned to show side-by-side diff.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-1.7.10-publiee.html</guid><pubDate>Sun, 15 Apr 2012 22:52:00 GMT</pubDate></item><item><title>Git 1.7.9 publiée</title><link>http://gitfr.net/posts/git-1.7.9-publiee.html</link><description>&lt;div&gt;&lt;p&gt;La version 1.7.9 est sortie le 27 janvier avec plusieurs évolutions
intéressantes : &lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Un meilleur support des gros fichiers (Git n'étant pas réputé pour ça).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;La signature des commits, qui fait suite au piratage de la machine qui 
  héberge le dépôt Git du projet Linux de Linus Torvalds.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;L'internationalisation est maintenant activée (honte à moi, toujours pas
  démarré le projet de traduction en Français).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;La possibilité d'ajouter une description à une branche.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;L'option &lt;code&gt;--no-edit&lt;/code&gt; quand on amende un commit. &lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Je vous laisse comme d'habitude lire le changelog mais je trouve cette version
bien excitente ! J'attends la mise à jour sur ma machine pour faire quelques
tests.&lt;/p&gt;
&lt;h4&gt;Le changelog&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;gitk updates accumulated since early 2011.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;git-gui updated to 0.16.0.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;git-p4 (in contrib/) updates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Git uses gettext to translate its most common interface messages
  into the user's language if translations are available and the
  locale is appropriately set. Distributors can drop new PO files
  in po/ to add new translations.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The code to handle username/password for HTTP transactions used in
  "git push" &amp;amp; "git fetch" learned to talk "credential API" to
  external programs to cache or store them, to allow integration with
  platform native keychain mechanisms.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The input prompts in the terminal use our own getpass() replacement
  when possible. HTTP transactions used to ask for the username without
  echoing back what was typed, but with this change you will see it as
  you type.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The internals of "revert/cherry-pick" have been tweaked to prepare
  building more generic "sequencer" on top of the implementation that
  drives them.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rev-parse FETCH_HEAD" after "git fetch" without specifying
  what to fetch from the command line will now show the commit that
  would be merged if the command were "git pull".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git add" learned to stream large files directly into a packfile
  instead of writing them into individual loose object files.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git checkout -B &lt;current branch&gt; &lt;elsewhere&gt;" is a more intuitive
  way to spell "git reset --keep &lt;elsewhere&gt;".&lt;/elsewhere&gt;&lt;/elsewhere&gt;&lt;/current&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git checkout" and "git merge" learned "--no-overwrite-ignore" option
  to tell Git that untracked and ignored files are not expendable.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit --amend" learned "--no-edit" option to say that the
  user is amending the tree being recorded, without updating the
  commit log message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit" and "git reset" re-learned the optimization to prime
  the cache-tree information in the index, which makes it faster to
  write a tree object out after the index entries are updated.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit" detects and rejects an attempt to stuff NUL byte in
  the commit log message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit" learned "-S" to GPG-sign the commit; this can be shown
  with the "--show-signature" option to "git log".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;fsck and prune are relatively lengthy operations that still go
  silent while making the end-user wait. They learned to give progress
  output like other slow operations.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The set of built-in function-header patterns for various languages
  knows MATLAB.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log --format='&lt;format&gt;'" learned new %g[nNeE] specifiers to
  show information from the reflog entries when walking the reflog
  (i.e. with "-g").&lt;/format&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git pull" can be used to fetch and merge an annotated/signed tag,
  instead of the tip of a topic branch. The GPG signature from the
  signed tag is recorded in the resulting merge commit for later
  auditing.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log" learned "--show-signature" option to show the signed tag
  that was merged that is embedded in the merge commit. It also can
  show the signature made on the commit with "git commit -S".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git branch --edit-description" can be used to add descriptive text
  to explain what a topic branch is about.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fmt-merge-msg" learned to take the branch description into
  account when preparing a merge summary that "git merge" records
  when merging a local branch.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git request-pull" has been updated to convey more information
  useful for integrators to decide if a topic is worth merging and
  what is pulled is indeed what the requestor asked to pull,
  including:&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;the tip of the branch being requested to be merged;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;the branch description describing what the topic is about;&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;the contents of the annotated tag, when requesting to pull a tag.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git pull" learned to notice 'pull.rebase' configuration variable,
   which serves as a global fallback for setting 'branch.&lt;name&gt;.rebase'
   configuration variable per branch.&lt;/name&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git tag" learned "--cleanup" option to control how the whitespaces
   and empty lines in tag message are cleaned up.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" learned to show side-by-side diff.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-1.7.9-publiee.html</guid><pubDate>Sat, 28 Jan 2012 21:13:00 GMT</pubDate></item><item><title>Comportement par défaut du git push</title><link>http://gitfr.net/posts/comportement-par-defaut-du-git-push.html</link><description>&lt;div&gt;&lt;p&gt;Git est hautement configurable, avec des dizaines de variables possibles
dans le fichier de configuration, la longueur de l'aide de l'option config
donnant d'ailleurs quelques vertiges. Au lieu de faire un (très) long billet
sur les différentes possibilités de configuration, je vous propose plutôt
des billets courts sur un thème particulier de la configuration.&lt;/p&gt;
&lt;p&gt;Pour ce premier billet, je souhaite aborder le comportement de Git lors d'un
push. Ce qui ont suivis l'atelier Git à Bordeaux ne seront pas étonnés par ce
choix : ayant totalement oublié cette option, je ne comprenais pas que ma
machine ne se comporte pas comme ceux des participants à l'atelier (ma
crédibilité en tant qu'expert Git en a sérieusement pâti :).&lt;/p&gt;
&lt;p&gt;Commençons par lire le manuel sur l'option push.default :&lt;/p&gt;
&lt;p&gt;Defines the action git push should take if no refspec is given on the command
line, no refspec is configured in the remote, and no refspec is implied by any
of the options given on the command line. Possible values are: &lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;nothing - do not push anything.&lt;/li&gt;
&lt;li&gt;matching - push all matching branches. All branches having the same name in
both ends are considered to be matching. This is the default.&lt;/li&gt;
&lt;li&gt;upstream - push the current branch to its upstream branch.&lt;/li&gt;
&lt;li&gt;tracking - deprecated synonym for upstream.&lt;/li&gt;
&lt;li&gt;current - push the current branch to a branch of the same name.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Comme vous pouvez le constater, si aucun refspec n'est fourni (en ligne de
commande ou par configuration) le comportement par défaut est de pousser
toutes les branches qui existent en local et sur le serveur. Cela veut donc
dire que si vous tapez la commande &lt;code&gt;git push&lt;/code&gt;, vous allez mettre à jour sur le
serveur toutes les branches que vous avez modifié en local, ce qui est rarement
souhaitable. Il faudra indiquer le refspec complet &lt;code&gt;git push origin mybranch&lt;/code&gt;
pour éviter cela.&lt;/p&gt;
&lt;p&gt;C'est pour cette raison que je configure Git avec l'option upstream :&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;$ git config --global push.default upstream&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;2 avantages immédiats :&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Il ne pousse que la branche courante.&lt;/li&gt;
&lt;li&gt;Il m'oblige à faire un &lt;em&gt;tracking&lt;/em&gt;, ce qui est bien utile.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Maintenant, si vous tentez de pousser la branche &lt;em&gt;test&lt;/em&gt; avec la commande
&lt;code&gt;git push&lt;/code&gt; sans tracking préalable, Git vous répond :&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;fatal: The current branch test has no upstream branch.
To push the current branch and set the remote as upstream, use&lt;/p&gt;
&lt;p&gt;git push --set-upstream origin test&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Suivez le conseil de Git en tapant la commande ci dessus pour suivre votre
la branche distante test sur origin avec votre branche locale test. Vous verrez
immédiatement un changement sur votre shell avec l'information &lt;strong&gt;u=&lt;/strong&gt;, qui 
indique que vos deux branches sont synchronisées.&lt;/p&gt;
&lt;h4&gt;Rappel sur le tracking&lt;/h4&gt;
&lt;p&gt;Le tracking consiste à &lt;em&gt;associer&lt;/em&gt; une branche locale et une branche distante
(dite &lt;em&gt;upstream&lt;/em&gt; dans la terminologie Git), ce qui informe Git de la branche
distante à utiliser lors d'un &lt;code&gt;git pull&lt;/code&gt;. Si vous ne comprenez pas pourquoi
vous n'êtes pas plus souvent confronté à cette notion de tracking, c'est tout
simplement que Git le fait pour vous la plupart du temps :&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Git suit la branche master lors d'un clone.&lt;/li&gt;
&lt;li&gt;Git suit la branche que vous venez de créer si celle ci existe déjà sur le
   serveur &lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;C'est donc (la plupart du temps) dans le cas ou créer une nouvelle branche en
locale sans existance préalable sur le serveur que vous devez explicitement
faire cette association.&lt;/p&gt;&lt;/div&gt;</description><category>git</category><category>git-push</category><category>git-pull</category><category>refspec</category><category>configuration</category><guid>http://gitfr.net/posts/comportement-par-defaut-du-git-push.html</guid><pubDate>Sun, 11 Dec 2011 23:19:00 GMT</pubDate></item><item><title>Git 1.7.8 publiée</title><link>http://gitfr.net/posts/git-1.7.8-publiee.html</link><description>&lt;div&gt;&lt;p&gt;La version 1.7.8 est sortie le 2 décembre. Comme vous pouvez constater, le
changelog est assez gros avec surtout de nouvelles options pour plusieurs
commandes et l'habituel lot de corrections.&lt;/p&gt;
&lt;h4&gt;Le changelog&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Some git-svn, git-gui, git-p4 (in contrib) and msysgit updates.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Updates to bash completion scripts.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The build procedure has been taught to take advantage of computed
  dependency automatically when the complier supports it.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The date parser now accepts timezone designators that lack minutes
  part and also has a colon between "hh:mm".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The contents of the /etc/mailname file, if exists, is used as the
  default value of the hostname part of the committer/author e-mail.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git am" learned how to read from patches generated by Hg.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git archive" talking with a remote repository can report errors
  from the remote side in a more informative way.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git branch" learned an explicit --list option to ask for branches
  listed, optionally with a glob matching pattern to limit its output.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git check-attr" learned "--cached" option to look at .gitattributes
  files from the index, not from the working tree.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Variants of "git cherry-pick" and "git revert" that take multiple
  commits learned to "--continue" and "--abort".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git daemon" gives more human readble error messages to clients
  using ERR packets when appropriate.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Errors at the network layer is logged by "git daemon".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git diff" learned "--minimal" option to spend extra cycles to come
  up with a minimal patch output.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git diff" learned "--function-context" option to show the whole
  function as context that was affected by a change.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git difftool" can be told to skip launching the tool for a path by
  answering 'n' to its prompt.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch" learned to honor transfer.fsckobjects configuration to
  validate the objects that were received from the other end, just like
  "git receive-pack" (the receiving end of "git push") does.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch" makes sure that the set of objects it received from the
  other end actually completes the history before updating the refs.
  "git receive-pack" (the receiving end of "git push") learned to do the
  same.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch" learned that fetching/cloning from a regular file on the
  filesystem is not necessarily a request to unpack a bundle file; the
  file could be ".git" with "gitdir: &lt;path&gt;" in it.&lt;/path&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git for-each-ref" learned "%(contents:subject)", "%(contents:body)"
  and "%(contents:signature)". The last one is useful for signed tags.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git grep" used to incorrectly pay attention to .gitignore files
  scattered in the directory it was working in even when "--no-index"
  option was used. It no longer does this. The "--exclude-standard"
  option needs to be given to explicitly activate the ignore
  mechanism.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git grep" learned "--untracked" option, where given patterns are
   searched in untracked (but not ignored) files as well as tracked
   files in the working tree, so that matches in new but not yet
   added files do not get missed.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The recursive merge backend no longer looks for meaningless
  existing merges in submodules unless in the outermost merge.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git log" and friends learned "--children" option.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git ls-remote" learned to respond to "-h"(elp) requests.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"mediawiki" remote helper can interact with (surprise!) MediaWiki
  with "git fetch" &amp;amp; "git push".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git merge" learned the "--edit" option to allow users to edit the
  merge commit log message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rebase -i" can be told to use special purpose editor suitable
  only for its insn sheet via sequence.editor configuration variable.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git send-email" learned to respond to "-h"(elp) requests.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git send-email" allows the value given to sendemail.aliasfile to begin
  with "~/" to refer to the $HOME directory.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git send-email" forces use of Authen::SASL::Perl to work around
  issues between Authen::SASL::Cyrus and AUTH PLAIN/LOGIN.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git stash" learned "--include-untracked" option to stash away
  untracked/ignored cruft from the working tree.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git submodule clone" does not leak an error message to the UI
  level unnecessarily anymore.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git submodule update" learned to honor "none" as the value for
  submodule.&lt;name&gt;.update to specify that the named submodule should
  not be checked out by default.&lt;/name&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When populating a new submodule directory with "git submodule init",
  the $GIT_DIR metainformation directory for submodules is created inside
  $GIT_DIR/modules/&lt;name&gt;/ directory of the superproject and referenced
  via the gitfile mechanism. This is to make it possible to switch
  between commits in the superproject that has and does not have the
  submodule in the tree without re-cloning.&lt;/name&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" leaked unescaped control characters from syntax hiliter
  outputs.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" can be told to give custom string at the end of the HTML
  HEAD element.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"gitweb" now has its own manual pages.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;HTTP transport did not use pushurl correctly, and also did not tell
  what host it is trying to authenticate with when asking for
  credentials.
  (merge deba493 jk/http-auth later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git blame" was aborted if started from an uncommitted content and
  the path had the textconv filter in effect.
  (merge 8518088 ss/blame-textconv-fake-working-tree later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Adding many refs to the local repository in one go (e.g. "git fetch"
  that fetches many tags) and looking up a ref by name in a repository
  with too many refs were unnecessarily slow.
  (merge 17d68a54d jp/get-ref-dir-unsorted later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Report from "git commit" on untracked files was confused under
  core.ignorecase option.
  (merge 395c7356 jk/name-hash-dirent later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git merge" did not understand ":/&lt;pattern&gt;" as a way to name a commit.&lt;/pattern&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git push" on the receiving end used to call post-receive and post-update
  hooks for attempted removal of non-existing refs.
  (merge 160b81ed ph/push-to-delete-nothing later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Help text for "git remote set-url" and "git remote set-branches"
  were misspelled.
  (merge c49904e fc/remote-seturl-usage-fix later to maint).
  (merge 656cdf0 jc/remote-setbranches-usage-fix later to maint).&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-1.7.8-publiee.html</guid><pubDate>Sun, 11 Dec 2011 23:13:00 GMT</pubDate></item><item><title>Comprendre le refspec</title><link>http://gitfr.net/posts/0079-comprendre_le_refspec.html</link><description>&lt;div&gt;&lt;p&gt;Si vous avez déjà travaillé avec un dépôt distant, vous savez comment se
comporte Git : il vous indique les références du dépôt distant en
ajoutant l'espace de nom &lt;code&gt;origin&lt;/code&gt; (dans le cas d'un clone) ou le nom que
vous lui donnez si vous l'avez ajouter avec la commande &lt;code&gt;git-remote add&lt;/code&gt;.
Mais pourquoi Git a t'il ce comportement ?&lt;/p&gt;
&lt;p&gt;La réponse se trouve dans le &lt;strong&gt;refspec&lt;/strong&gt;, nom un peu barbare, qui pourrait
se traduire en bon français par : «comment je gère les dépôts distants
chef ?». Soit ce refspec est spécifié en ligne de commande au moment
de l'action (fetch ou push), soit elle se trouve dans le fichier de
configuration du dépôt.&lt;/p&gt;
&lt;h4&gt;Petit rappel&lt;/h4&gt;
&lt;p&gt;Il est &lt;strong&gt;impossible&lt;/strong&gt; de modifer directement un dépôt distant, vous devez
obligatoirement modifier le dépôt local puis pousser vos commits
sur le dépôt distant. Autrement dit, modifier un dépôt consiste à
synchroniser les bases de données.&lt;/p&gt;
&lt;p&gt;C'est là que le refspec rentre en jeu, il permet de définir les branches
concernées au moment de la synchronisation.&lt;/p&gt;
&lt;h4&gt;Clonage&lt;/h4&gt;
&lt;p&gt;Si vous joué rarement avec le refspec, c'est que vous travaillé la plupart
du temps sur un dépôt local que vous avez créé avec la commande &lt;code&gt;git-clone&lt;/code&gt;.
Ce dernier génère un refspec générique suffisant. Editez le fichier
&lt;code&gt;.git/config&lt;/code&gt; et vous verrez ces lignes :&lt;/p&gt;
&lt;div class="code"&gt;&lt;pre&gt;&lt;span class="k"&gt;[remote "origin"]&lt;/span&gt;
&lt;span class="na"&gt;fetch&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="s"&gt;+refs/heads/*:refs/remotes/origin/*&lt;/span&gt;
&lt;span class="na"&gt;url&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="s"&gt;/path/to/repos&lt;/span&gt;
&lt;/pre&gt;&lt;/div&gt;


&lt;p&gt;Une explication rapide : le dépôt distant qui se trouve à l'url spécifié
s'appelle en local &lt;em&gt;origin&lt;/em&gt; et on associe à chaque référence de branche
du dépôt distant une référence de branche locale préfixée par &lt;em&gt;origin&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;Mais un deuxième rappel avant d'aller plus loin.&lt;/p&gt;
&lt;h4&gt;Structure des références&lt;/h4&gt;
&lt;p&gt;La gestion des références est d'une simplicité affligeante. Tout est contenu
dans le répertoire &lt;code&gt;.git/refs&lt;/code&gt;. Un répertoire étant un espace de nom, un
fichier une référence. Pour fonctionner, Git à besoin de 2 espaces de nom
bien particulier :&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;heads&lt;/em&gt; pour stocker les références locales de type branche.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;em&gt;tags&lt;/em&gt; pour stocker les références locales de type tag.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Vous ne faites pas attention à ces répertoires en temps normal car les
commandes Git savent pertinamment que les références sont à ces endroits
précis.&lt;/p&gt;
&lt;h4&gt;Explication&lt;/h4&gt;
&lt;p&gt;Si je vous dis qu'un refspec s'écrit toujours &lt;code&gt;source:destination&lt;/code&gt;, la ligne
suivante ne doit pas vous poser trop de soucis. Examinons la de plus prêt :&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;fetch = +refs/heads/&lt;em&gt;:refs/remotes/origin/&lt;/em&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Le mot clé fetch indique que c'est lu à chaque utilisation de cette commande
  Le sens de transfert est du dépôt distant (la source) vers le dépôt local
  (la destination).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Le symbole &lt;code&gt;+&lt;/code&gt; signifie qu'on autorise les récuparation &lt;em&gt;non fast-forword&lt;/em&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;le reste de la ligne est assez explicite. Pour toutes les références se
  trouvant dans le chemin &lt;code&gt;refs/heads/&lt;/code&gt; dans le dépôt distant, on associe une
  référence dans le chemin &lt;code&gt;refs/remotes/origin/&lt;/code&gt; dans le dépot local. &lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Si par exemple le dépôt distant a 3 branches qui se nomment &lt;code&gt;riri&lt;/code&gt;, &lt;code&gt;fifi&lt;/code&gt; et
&lt;code&gt;loulou&lt;/code&gt;, vous aurez donc en local 3 branches &lt;code&gt;origin/riri&lt;/code&gt;, &lt;code&gt;origin/fifi&lt;/code&gt; et
&lt;code&gt;origin/loulou&lt;/code&gt;.&lt;/p&gt;
&lt;h4&gt;Importance du refspec&lt;/h4&gt;
&lt;p&gt;Il est important de maitriser le refspec car tout part de là, c'est en fonction
des références à récupérer ou à pousser que Git détermine les objets à 
synchroniser. Si je demande à récupérer toutes les références distantes, je demande
implicitement tous les objets accessibles par ces références, donc toute la base.
Au contraire, si le refspec implique des références précises, seuls les objets
nécessaires à ces références seront impactés.&lt;/p&gt;
&lt;h4&gt;Ajout d'un dépôt distant&lt;/h4&gt;
&lt;p&gt;La commande &lt;code&gt;git-remote&lt;/code&gt; permet de manipuler les dépôts distants, avec
notamment la commande &lt;code&gt;git-remote add&lt;/code&gt; pour en ajouter. Ce dernier se comporte
comme la commande &lt;code&gt;git-clone&lt;/code&gt;, en configurant le refspec au plus
large (toutes les références donc).&lt;/p&gt;
&lt;h4&gt;refspec en ligne de commande&lt;/h4&gt;
&lt;p&gt;Le refspec se manipule aussi en ligne de commande, ce qui permet de passer
outre la configuration ou d'utiliser Git sans configuration préalable.&lt;/p&gt;
&lt;p&gt;Quelques exemples :&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;git fetch origin +pu:tmp&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Récupére la référence &lt;em&gt;pu&lt;/em&gt; et les objets associés et appeles la &lt;em&gt;tmp&lt;/em&gt;
en local.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;git push HEAD:master&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Pousse la branche actuelle sur la branche distante master.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;git push origin master:refs/heads/qa/experimental&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Pousse la branche master sur la branche distante expérimental. Nous
utilisons ici le nom complet de la référence. &lt;/p&gt;
&lt;h4&gt;Le refspec :&lt;/h4&gt;
&lt;p&gt;Refspec particulier qui indique de mettre à jour toutes les branches
distantes existantes qui ont un nom identique aux branches locales. C'est en
fait le refspec utilisé par défaut au push quand nous en spécifions pas.&lt;/p&gt;
&lt;h4&gt;Effacer une référence distante&lt;/h4&gt;
&lt;p&gt;Autre cas particulier. Si vous souhaitez par exemple effacer la référence
distante test si vous souhaitez effacer une référence distante,
il faut taper :&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;git push :test&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Autrement dit, je pousse une référence vide sur la référence test.&lt;/p&gt;
&lt;h4&gt;Associer branche locale / branche distante&lt;/h4&gt;
&lt;p&gt;Comme nous l'avons vu, le refspec en ligne de commande permet d'associer
temporairement une branche locale et une branche sur le dépôt distant. Mais
comment associer ces deux branches dans la configuration et éviter ainsi de
le répêter à chaque fois ? Tout se situe encore dans le fichier
&lt;code&gt;.git/config&lt;/code&gt; du dépôt local :&lt;/p&gt;
&lt;div class="code"&gt;&lt;pre&gt;&lt;span class="p"&gt;[&lt;/span&gt;&lt;span class="n"&gt;branch&lt;/span&gt; &lt;span class="s"&gt;"master"&lt;/span&gt;&lt;span class="p"&gt;]&lt;/span&gt;                                                                                   
&lt;span class="n"&gt;remote&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;origin&lt;/span&gt;                                                                             
&lt;span class="n"&gt;merge&lt;/span&gt; &lt;span class="o"&gt;=&lt;/span&gt; &lt;span class="n"&gt;refs&lt;/span&gt;&lt;span class="o"&gt;/&lt;/span&gt;&lt;span class="n"&gt;heads&lt;/span&gt;&lt;span class="o"&gt;/&lt;/span&gt;&lt;span class="n"&gt;master&lt;/span&gt;
&lt;/pre&gt;&lt;/div&gt;


&lt;p&gt;Qui veut dire : associe la branche master du dépôt &lt;em&gt;origin&lt;/em&gt; à ma référence
locale &lt;code&gt;master&lt;/code&gt;. Et oh surprise, la ligne est utilisée pour la commande
&lt;code&gt;merge&lt;/code&gt;, ce qui est tout à faire logique. la commande &lt;code&gt;git-fetch&lt;/code&gt; ne fait que
récupérer les objets et associer des références. Si vous voulez mélanger
votre travail avec le travail distant, il faut donc faire un merge.&lt;/p&gt;
&lt;p&gt;Petite remarque, c'est uniquement quand vous avez spécifiée une branche
distante (&lt;em&gt;upstream&lt;/em&gt; dans le vocabulaire Git) que votre shell vous indiquera
si vous avez des commits en avance ou en retard (logique non ?). Cela arrive
quand vous avez créé une nouvelle branche locale mais sans l'avoir poussée
sur le dépôt distant.&lt;/p&gt;
&lt;h4&gt;pull = fetch + merge&lt;/h4&gt;
&lt;p&gt;Vous devez maintenant comprendre pourquoi on dit que la commande &lt;code&gt;git-pull&lt;/code&gt;
n'est en fait qu'un raccouri pour :&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;git-fetch&lt;/li&gt;
&lt;li&gt;git-merge origin/master&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;A quoi sert le fetch ?&lt;/h4&gt;
&lt;p&gt;La commande &lt;code&gt;git-pull&lt;/code&gt; est une commodité car c'est dans 90% des cas ce que l'on
souhaite faire. Mais le merge est seulement une des nombreuses possibilités que
vous offre Git. La beauté du travail collaboratif avec Git est que cette branche
distante est en fait une branche locale (c'est uniquement la référence qui est
distante), et que vous pouvez donc faire ce que vous souhaitez avec, comme
cherrypicker des commits, faire un rebase... ou un merge. C'est à vous de voir
ce qui vous intéresse. C'est la raison pour laquelle des personnes disent sur
le Net de ne pas faire que des fetch.&lt;/p&gt;
&lt;p&gt;Notez qu'il existe l'option &lt;code&gt;--rebase&lt;/code&gt; pour rebaser automatiquement votre
branche locale sur la branche distante (ce qui est la aussi un scénario de
base).&lt;/p&gt;
&lt;h4&gt;Conclusion&lt;/h4&gt;
&lt;p&gt;Je pense avoir fait le tour. Comme d'habitude je vous conseille de de lire
les pages du manuel (git-clone, git-fetch, git-push et git-pull).&lt;/p&gt;&lt;/div&gt;</description><category>git</category><category>refspec</category><category>git-clone</category><category>git-remote</category><category>git-fetch</category><category>git-pull</category><category>git-push</category><guid>http://gitfr.net/posts/0079-comprendre_le_refspec.html</guid><pubDate>Sun, 09 Oct 2011 17:53:00 GMT</pubDate></item><item><title>Git 1.7.7 publiée</title><link>http://gitfr.net/posts/git-1.7.7-publiee.html</link><description>&lt;div&gt;&lt;p&gt;Reprenons la bonne habitude d'annoncer les nouvelles versions. Rien de bien
méchant dans cette 1.7.7, il faut attendre la 1.8 je pense pour des plus gros
changements.&lt;/p&gt;
&lt;h4&gt;Le changelog&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;The scripting part of the codebase is getting prepared for i18n/l10n.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Interix, Cygwin and Minix ports got updated.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Various updates to git-p4 (in contrib/), fast-import, and git-svn.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Gitweb learned to read from /etc/gitweb-common.conf when it exists,
  before reading from gitweb_config.perl or from /etc/gitweb.conf
  (this last one is read only when per-repository gitweb_config.perl
  does not exist).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Various codepaths that invoked zlib deflate/inflate assumed that these
  functions can compress or uncompress more than 4GB data in one call on
  platforms with 64-bit long, which has been corrected.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Git now recognizes loose objects written by other implementations that
  use a non-standard window size for zlib deflation (e.g. Agit running on
  Android with 4kb window). We used to reject anything that was not
  deflated with 32kb window.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Interaction between the use of pager and coloring of the output has
  been improved, especially when a command that is not built-in was
  involved.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git am" learned to pass the "--exclude=&lt;path&gt;" option through to underlying
  "git apply".&lt;/path&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;You can now feed many empty lines before feeding an mbox file to
  "git am".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git archive" can be told to pass the output to gzip compression and
  produce "archive.tar.gz".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git bisect" can be used in a bare repository (provided that the test
  you perform per each iteration does not need a working tree, of
  course).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The length of abbreviated object names in "git branch -v" output
  now honors the core.abbrev configuration variable.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git check-attr" can take relative paths from the command line.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git check-attr" learned an "--all" option to list the attributes for a
  given path.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git checkout" (both the code to update the files upon checking out a
  different branch and the code to checkout a specific set of files) learned
  to stream the data from object store when possible, without having to
  read the entire contents of a file into memory first. An earlier round
  of this code that is not in any released version had a large leak but
  now it has been plugged.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git clone" can now take a "--config key=value" option to set the
  repository configuration options that affect the initial checkout.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git commit &lt;paths&gt;..." now lets you feed relative pathspecs that
  refer to outside your current subdirectory.&lt;/paths&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git diff --stat" learned a --stat-count option to limit the output of
  a diffstat report.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git diff" learned a "--histogram" option to use a different diff
  generation machinery stolen from jgit, which might give better
  performance.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git diff" had a weird worst case behaviour that can be triggered
  when comparing files with potentially many places that could match.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git fetch", "git push" and friends no longer show connection
  errors for addresses that couldn't be connected to when at least one
  address succeeds (this is arguably a regression but a deliberate
  one).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git grep" learned "--break" and "--heading" options, to let users mimic
  the output format of "ack".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git grep" learned a "-W" option that shows wider context using the same
  logic used by "git diff" to determine the hunk header.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Invoking the low-level "git http-fetch" without "-a" option (which
  git itself never did---normal users should not have to worry about
  this) is now deprecated.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The "--decorate" option to "git log" and its family learned to
  highlight grafted and replaced commits.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git rebase master topci" no longer spews usage hints after giving
  the "fatal: no such branch: topci" error message.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The recursive merge strategy implementation got a fairly large
  fix for many corner cases that may rarely happen in real world
  projects (it has been verified that none of the 16000+ merges in
  the Linux kernel history back to v2.6.12 is affected with the
  corner case bugs this update fixes).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git stash" learned an "--include-untracked option".&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git submodule update" used to stop at the first error updating a
  submodule; it now goes on to update other submodules that can be
  updated, and reports the ones with errors at the end.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git push" can be told with the "--recurse-submodules=check" option to
  refuse pushing of the supermodule, if any of its submodules'
  commits hasn't been pushed out to their remotes.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git upload-pack" and "git receive-pack" learned to pretend that only a
  subset of the refs exist in a repository. This may help a site to
  put many tiny repositories into one repository (this would not be
  useful for larger repositories as repacking would be problematic).&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;"git verify-pack" has been rewritten to use the "index-pack" machinery
  that is more efficient in reading objects in packfiles.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;test scripts for gitweb tried to run even when CGI-related perl modules
  are not installed; they now exit early when the latter are unavailable.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;&lt;/div&gt;</description><category>git</category><category>release</category><guid>http://gitfr.net/posts/git-1.7.7-publiee.html</guid><pubDate>Sat, 01 Oct 2011 10:34:00 GMT</pubDate></item></channel></rss>